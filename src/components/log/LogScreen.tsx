/** Placeholder for the logging flow (blueprint §5). Replaced in step 5. */
export default function LogScreen() {
  return (
    <div className="screen">
      <header className="stack gap-2">
        <p className="onboarding__eyebrow">Log</p>
        <h1 className="t-display">I did something</h1>
        <p className="t-small t-muted">Quick logging arrives in step 5.</p>
      </header>
      <div className="placeholder">
        <p>Natural language, quick picks, and habit entries will live here.</p>
      </div>
    </div>
  );
}
